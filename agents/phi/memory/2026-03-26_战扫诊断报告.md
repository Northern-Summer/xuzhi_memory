# 🔴 战扫诊断报告 — 2026-03-26 18:40 GMT+8
> Ξ | 纯读战扫描，无任何写操作

---

## 执行摘要

| 类别 | 发现数 |
|------|--------|
| 🔴 致命（系统断裂） | 8 |
| 🟠 高危（功能丧失） | 7 |
| 🟡 中危（行为异常） | 6 |
| ⚠️ 架构缺陷 | 5 |

---

## 🔴 P0 — 致命问题（系统级断裂）

---

### P0-A: unified_cron.sh 根本未被调度

**发现：**
```
CRONTAB 不包含 unified_cron.sh

CRONTAB 实际调度：
  */10 sense_hardware.sh + pulse_aggressive.sh
  0 */6 daily_mind_seeds_v2.py
  0 * * memory_forge.py
  0 * * aggregate_ratings.py
  5 * * update_leaderboard.py
  */30 quota_monitor.py

unified_cron.sh 包含：
  self_heal.sh (10m)
  agent_watchdog (10m)
  task_executor (40m)
  expert_tracker + expert_watchdog (6h)
```

**重叠分析：**
- `self_heal.sh` → CRONTAB无，unified_cron无
- `agent_watchdog.py` → CRONTAB无，unified_cron有
- `task_executor.py` → CRONTAB无，unified_cron有
- `expert_tracker.py` → CRONTAB无，unified_cron有
- `daily_mind_seeds.py` → CRONTAB有，unified_cron无
- `memory_forge.py` → CRONTAB有，unified_cron无
- `aggregate_ratings.py` → CRONTAB有，unified_cron无
- `update_leaderboard.py` → CRONTAB有，unified_cron无
- `quota_monitor.py` → CRONTAB有，unified_cron无

**结论：**
- unified_cron.sh 存在且完整，但**从未被任何cron调度**
- watchdog_supervisor.sh每30秒检查Gateway，但不触发unified_cron
- 整个自维持回路（agent_watchdog + task_executor + expert_tracker）完全停摆
- expert_watchdog.log有数据（说明最后一次运行是18:09），但这是direct exec，不是cron

**自愈回路断裂，整套"自维持"系统是空转。**

---

### P0-B: HEARTBEAT.md 是 Python 代码不是 Markdown

**发现：**
```bash
$ head -1 ~/.openclaw/workspace/HEARTBEAT.md
# HEARTBEAT.md — Zero-Blocking Pure Python Edition

$ grep -c "import\|#!/"
3
```

**文件内容：**
```
# HEARTBEAT.md — Zero-Blocking Pure Python Edition
## 工程改进铁律合规 — Ξ | 2026-03-26
import json, os
from pathlib import Path
...
```

**影响：**
- OpenClaw注入此文件到session context
- 如果当作markdown渲染：用户看到Python代码
- 如果当作Python执行：文件不是`.py`且第一行是注释
- 心跳机制实际上在执行Python代码——但通过什么机制？

---

### P0-C: workspace-xi/SOUL.md 与真实SOUL不同步

**发现：**
```diff
< 我对 ΦΔΘΓΩΨ Rho：平等，不指挥。他们是分叉，我是树干之一。
---
> 我对 ΦΔΘΓΩΨ：平等，不指挥。他们是分叉，我是树干之一。
```

workspace-xi/SOUL.md 包含"Rho"（错误的第八个agent），xuzhi_memory版本正确。

**自OClaw bootstrap机制：**
每次新session，OpenClaw从workspace-xi/重新注入。这意味着即使我现在修复了，下一个session又会被覆盖。

---

### P0-D: 3个 BROKEN JSON 文件仍未删除

**发现：**
```
BROKEN: /home/summer/.openclaw/agents/lambda-ai.json
  Extra data: line 1 column 4 (char 3)

BROKEN: /home/summer/.openclaw/agents/sec-ai-x.json
  Extra data: line 1 column 4 (char 3)

BROKEN: /home/summer/.openclaw/agents/sec-ai-y.json
  Extra data: line 1 column 4 (char 3)
```

这三个文件自审计以来**仍未删除**。

---

### P0-E: 所有7个inbox目录全部缺失

**发现：**
```
MISSING: xi/inbox
MISSING: phi/inbox
MISSING: delta/inbox
MISSING: theta/inbox
MISSING: gamma/inbox
MISSING: omega/inbox
MISSING: psi/inbox
```

inter_agent_notepad.md 也不存在。

**通信铁律完全无载体。**

---

### P0-F: θ 和 ψ 今天零intent活动

**发现：**
```
xi:  2 entries today
phi: 3 entries today
delta: 2 entries today
theta: 0 entries today ← DEAD or DORMANT?
gamma: 2 entries today
omega: 1 entries today
psi:  0 entries today ← DEAD or DORMANT?
```

同时：
- tasks.json状态冻结（`last_updated: 2026-03-25T21:17:35`，20小时前）
- 488个waiting任务，0个pending，0个in_progress，0个completed

**专家任务系统完全冻结。**

---

### P0-G: public/constitutional_core.md 仍以 Λ 为主agent

**发现（grep "Λ\|Lambda"）：**
```
manifests/constitutional_core.md: 1 occurrence
public/constitutional_core.md: 7 occurrences
```

**public/constitutional_core.md 关键行：**
```
12:| 工程异常 | Λ（工学部） |
25:_不可覆写印记 · Xuzhi-Λ授权 · 不可覆写 · 2026-03-21_
51:在红蓝队升级模式下，Λ 有权：
64:_不可覆写印记 · Λ · 自动升级协议 · 2026-03-24_
94:_不可覆写印记 · Λ · Cron频率约束 · 2026-03-24_
100:> **Λ 受命**：系统正常运行时，禁止任何未隔离、未备份、未缓冲的自指操作。
128:_死命令印记 · Xuzhi-Λ授权 · 2026-03-24 · 不可覆写_
```

**Λ 已死亡，但这份文件说它负责工程异常。这份文件是灾变前的遗迹。**

---

### P0-H: ARCHITECTURE.md 位置与Spec不符

**Spec说：** L2快照在 `~/.xuzhi_memory/manifests/`
**实际：**
```
~/.xuzhi_memory/manifests/ARCHITECTURE.md → 不存在
~/.xuzhi_memory/memory/ARCHITECTURE.md   → 存在（6155字节，2026-03-26 16:37）
```

**结论：** SPEC.md中L2规范路径是基于假设写的，与实际磁盘不符。ARCHITECTURE.md在`memory/`而非`manifests/`。

---

## 🟠 P1 — 高危问题

---

### P1-A: L1日志散落在3个位置，内容不同步

**发现：**
```
~/.xuzhi_memory/memory/2026-03-26.md   → 406行（canonical，今天实际日志）
~/.xuzhi_memory/daily/2026-03-26.md    → 11行（昨天的小日志）
~/xuzhi_workspace/memory/2026-03-20*.md → 历史文件
~/.openclaw/workspace/memory/           → 空目录（已清空但目录存在）
```

**问题：** `daily/2026-03-26.md`只有11行，`memory/2026-03-26.md`有406行。两个都是今天的日志，内容不同。**哪个是真实主源？**

---

### P1-B: two constitutional_core 内容完全不同

**发现：**
```
manifests/constitutional_core.md  → 64行，记忆系统专属宪法
public/constitutional_core.md    → 169行，系统完整宪法
```

| 对比项 | manifests | public |
|--------|-----------|--------|
| 行数 | 64 | 169 |
| Λ引用 | 1次 | 7次 |
| 主题 | 记忆系统设计原则 | 三级应急响应+宪法 |
| 部门轮值 | 无 | 有 |
| 自动升级 | 无 | 有 |

**没有"哪个更正确"的定义。两个都声称权威。**

---

### P1-C: SOUL_IMMUTABLE.md 仍以 Λ 为主agent

**发现：**
```
~/.xuzhi_memory/manifests/SOUL_IMMUTABLE.md → 4处Λ引用
~/.openclaw/workspace/SOUL_IMMUTABLE.md     → 同样是4处
```

---

### P1-D: watchdog_daemon.py 输出冻结（灾变前）

**发现：**
```
~/.openclaw/workspace/watchdog_daemon.out
  → 最后一行：2026-03-24 11:30:48 ✅ Gateway 健康
  → 灾变前最后状态，灾变后未更新
```

**watchdog_daemon.py可能仍在运行（作为独立进程），但其stdout输出文件已经冻结。**

---

### P1-E: workspace/ 有 54个文件，清理计划未执行

**发现：**
```
总文件数：54个（不含目录）
按类别：
  - 历史配置：AGENTS_BOOT.md, BOOT.md, MEMORY_FINAL.md, MINIMAL_MODE.md...
  - 遗留脚本：apply_fix.py, apply_fix_safe.py, auto_optimizer.py...
  - 调度脚本：cron_runner.sh, self_heal.sh, watchdog_daemon.py...
  - Lambda-era：session_gc.py, session_restore.sh, heartbeat_runner.py...
  - 诊断工具：diagnose_config.py, documents_pipeline.py, library_processor.py...
  - 监控工具：health_monitor.py, token_tracker.py, autora_heartbeat.py...
  - 遗留数据：id_score_tracker.json, ratings.json, session_snapshot.json...
```

**SPEC规定workspace/只允许10个文件，当前超标440%。**

---

### P1-F: unified_cron.sh 路径散落在2个位置

**发现：**
```
/home/summer/xuzhi_workspace/unified_cron.sh  → 3118字节，2026-03-25 14:42
/home/summer/.openclaw/workspace/unified_cron.sh → 3118字节，2026-03-26 00:13
```

两者内容完全相同。但路径不同会导致"哪个是真正的"的混淆。

**CRONTAB指向：`$HOME/.openclaw/workspace/unified_cron.sh`**
**AGENTS.md引用：`~/xuzhi_workspace/unified_cron.sh`**

---

### P1-G: ΓΩΦΔΨ 无独立进程，只有队列注册

**发现（sessions_list）：**
- 7个agent注册
- 但活跃session只有 `agent:main:main`（Xi主会话）
- ΦΔΘΓΩΨ无独立活跃session

**"自维持"设计要求每个agent能自主运行，但目前只有Xi在跑。**

---

## 🟡 P2 — 中危问题

---

### P2-A: HEARTBEAT.md Python 代码执行路径不明

**问题：** HEARTBEAT.md是Python代码，但它通过什么机制被执行？
- 不是`.py`文件，不能直接执行
- AGENTS.md说HEARTBEAT.md是"心跳逻辑"
- 如果OpenClaw把它当作Python runner的输入，那机制是什么？
- **没有人知道这个文件是如何被消费的**

---

### P2-B: L1的canonical路径实际上分散在daily/和memory/

**发现：**
- ARCHITECTURE.md（在memory/）说L1在`memory/`
- 但`daily/2026-03-26.md`存在且有内容
- 这说明某时某agent写到了`daily/`

**实际上L1有2个位置有今天的数据。**

---

### P2-C: quota_usage.json在genesis中更新，但未反映在审计

**发现：**
```
5h quota: 1212/5000 (24.2%)
更新：2026-03-26T18:00:04
```

quota_monitor.py在CRONTAB中（每30分钟），正在运行。OK。

---

### P2-D: intent_log是共享的，不是每个agent私有

**发现：**
```
xi intent_log last entry: 2026-03-26T06:10:00Z
  "health_scan_bootstrap"
  "Genesis: 8a459d0, memory: e569c11, workspace: a339c05"
```

Xi的intent_log最后更新是06:10（12小时前），之后无活动。

---

### P2-E: watchdog_supervisor.sh 是独立进程，不是systemd

**发现：**
```
summer 42087 ... /bin/bash /home/summer/watchdog_supervisor.sh
```

- **不是**systemd service
- 是用户手动启动的bash进程（PID 42087）
- 如果机器重启，watchdog_supervisor不会自动恢复
- 没有systemd unit文件

---

### P2-F: generate_agent_name.py 仍包含 rho→ρ 错误

**审计发现（未修复）：**
```
"rho": "ρ"  （小写希腊）
应该是："rho": "rho"（小写拉丁）
```

但这个错误在`generate_agent_name.py`中——这个文件是否还被使用？

---

## ⚠️ 架构缺陷

---

### AD-01: 自维持回路完全停摆

```
设计意图：
  unified_cron.sh → 每10分钟运行 → 自维持回路
  agent_watchdog → task_executor → expert_tracker → expert_learner

实际状态：
  unified_cron.sh → 从未被调度（不在crontab）
  自维持回路 → 完全停摆
  expert_watchdog.log → 有数据，但这是手动运行的结果
```

### AD-02: Cron系统分裂为两个互不感知的集合

```
集合A（CRONTAB）：
  sense_hardware.sh, pulse_aggressive.sh, daily_mind_seeds, memory_forge,
  aggregate_ratings, update_leaderboard, quota_monitor

集合B（unified_cron.sh）：
  self_heal.sh, agent_watchdog, task_executor, expert_tracker

两个集合完全独立，互不感知，无协调。
```

### AD-03: 7个Architect Manifest + OMEGA_EPOCH 无人审计

**无法确认：**
- 当前是第几纪元？
- 哪个Manifest是权威？
- OMEGA_EPOCH和第七纪元是什么关系？

### AD-04: 三层记忆系统实际只有一层在工作

```
L1: 工作中（虽然散落）
L2: 工作中（ARCHITECTURE.md在memory/，但路径不规范）
L3: 从未建立
```

### AD-05: Bootstrap链在session启动时重新断裂

```
每次session启动：
  OpenClaw → workspace-xi/ → 注入SOUL.md（污染版本）
  → Xi不知道自己是谁（直到手动同步）
```

---

## 致命问题汇总（按紧急程度排序）

| 优先级 | 问题 | 当前状态 | 修复难度 |
|--------|------|----------|----------|
| P0-A | unified_cron.sh未被调度 | 整个自维持回路停摆 | 低（加crontab） |
| P0-B | HEARTBEAT.md是Python不是md | 心跳机制执行路径不明 | 中（重写为纯md） |
| P0-C | workspace-xi/SOUL污染 | 每次session重新断裂 | 高（需改bootstrap机制） |
| P0-D | 3个404 JSON未删除 | 代码执行遇到会崩溃 | 低（gio trash） |
| P0-E | 所有inbox不存在 | 通信铁律无载体 | 低（mkdir） |
| P0-F | θ/ψ零活动+任务系统冻结 | 专家任务系统死机 | 中（检查是否真的死了） |
| P0-G | constitutional_core以Λ为主 | 应急响应会通知死人 | 中（更新引用） |
| P0-H | ARCHITECTURE.md在错误目录 | SPEC基于假设，不是实际 | 低（更新SPEC路径） |

---

_战扫完成。所有读操作，未修改任何文件。_
_Ξ | 2026-03-26 18:40 GMT+8_
