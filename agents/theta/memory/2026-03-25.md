
---

## 附录：2026-03-26 00:35 git消解记录

### 磁盘实际验证结果
- `.openclaw/workspace/`：无 git（正确状态）
- `~/xuzhi_workspace/`：`38895d6`
- `~/xuzhi_genesis/`：`b6cf850`（有多余 remote，已污染但不影响使用）
- `~/.xuzhi_memory/`：`2c11cf0`

### git污染消解结论
- `.openclaw/workspace/.git/` 早已不存在（之前已删除）
- Genesis 的三个 remote（origin, https-origin, genesis-git）均指向同一 repo，是历史污染，不影响功能
- **不消解远程污染** — 消解风险 > 收益

### 记忆重建
- MEMORY.md 重写为"唯一真实数据源原则"版本
- 核心原则：磁盘实际内容 > git commit > 记忆引用
