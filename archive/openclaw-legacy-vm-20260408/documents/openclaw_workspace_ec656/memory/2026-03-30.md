# 2026-03-30 | Xi (Ξ)

## 关键问题

### [16:09] cron sessionTarget 被反复覆盖
- **现象**：`jobs.json` 中 `sessionTarget` 多次被改回 `isolated`
- **尝试修复**：
  1. 直接 write jobs.json → 被覆盖
  2. `openclaw cron update` 返回 `current`，但文件仍是 `isolated`
- **根因**：Gateway 内存缓存，未同步磁盘配置
- **状态**：❌ 未解决，需重启 Gateway 强制同步

### [16:15] xuzhi_memory Git push 失败
- **原因**：token1 已过期，无 push 权限
- **修复**：改用 token2（与 genesis/workspace 一致）
- **命令**：`git remote set-url origin https://<token2>@github.com/Northern-Summer/xuzhi_memory.git`
- **状态**：✅ 已修复

## 阻塞项
1. Gateway 配置缓存问题 → cron 无法使用 `current` sessionTarget
2. xi_pulse 脚本 timeout（120s 不够）
3. AI4S Weekly Researcher 连续报错（channel 配置问题）

## 待验证
- xuzhi_memory push 是否正常

## 血泪教训

**【2026-03-30 16:27】禁止重启 Gateway**
- 每次重启会留下不可恢复的断点
- Gateway 配置有内存缓存，但重启代价太大
- 正确做法：用 `cron update` API 修改配置，等待同步
- **宁可忍受配置延迟，绝不重启 Gateway**
